# Build artifacts
build/tidemark
build/tidemark_sim
build/*.o
*.o

# Python
__pycache__/
*.py[cod]
venv/
.venv/

# Local, per-install config written by setup_location.py (the bundled default
# in config.py is used when this is absent).
src/python/location.json

# Local deploy config (Pi host/dir)
deploy.env

# OS / editor cruft
.DS_Store
*.swp
